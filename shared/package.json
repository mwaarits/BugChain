{
  "name": "@gmtbuilder/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Off-chain submission hash format — single source of truth for wallet and backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.39"
  }
}
